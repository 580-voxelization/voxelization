# Stylized Voxelization

This project is based on the codebase of ray tracing assignment. Out additional works are mainly in following `.cpp` or `.h` files:

- `voxelized_mesh`
- `random_shader`

- `voxel_face_uniform_shader`
- `voxel_frame_shader`
- `voxel_uniform_shader`
- `main`